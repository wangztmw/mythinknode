export const DESCRIPTION = `Edit a file by exact string replacement. Use for precise, targeted changes.
- old_string must match exactly (including whitespace/indentation).
- Add replace_all: true to replace every occurrence.
- Prefer this over Write for small changes to existing files.`;
