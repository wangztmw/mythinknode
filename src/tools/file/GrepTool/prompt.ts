export const DESCRIPTION = `Search file contents using regex. Use for finding code patterns, references, definitions, error messages.
- Returns file paths with line numbers and matching lines.
- Excludes node_modules, .git, dist by default.
- Limited to 100 matches.`;
