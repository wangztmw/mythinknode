export const DESCRIPTION = `Write content to a file, overwriting if it exists. Use for creating new files or fully replacing existing files.
- Creates parent directories if they don't exist.
- For partial edits, use the Edit tool instead.`;
