export const DESCRIPTION = `Build a DAG of knowledge from a markdown document. Runs in the BACKGROUND — returns a job id immediately (one ingest at a time). Chunks the file, extracts knowledge atoms (concept/definition/theorem/formula/table/case) and their relations, merges into the global infermem DAG (semantic entity resolution + implicit edge inference included). Use InferQuery(action='status') to monitor progress, then query/read/walk to explore.`;
