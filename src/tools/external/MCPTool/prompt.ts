export const DESCRIPTION = `Call an external MCP (Model Context Protocol) tool. Use for accessing external services.
- serverName: the MCP server name as configured in .mcp.json
- toolName: the tool to call within that server
- arguments: JSON object with tool parameters`;
